# Review handoff: 2026-09-13

The product edit is 66 seconds. The engineering edit is approximately 97 seconds. Both are 1920×1080 H.264 MP4s without an audio stream, with burned-in narrative captions and matching editable SRT/VTT files. Generated files are in ignored `demo/video/output/`; `patchbay-voice-review.zip` bundles the videos, captions, contact sheets, actual note and verification report.

## Evidence

The selected capture is `runs/ios18-take`, using the real app on an iPhone 16 Pro simulator running iOS 18.5, a disposable source subset of Patchbay Go, and actual production server/pi calls through LiteLLM small. Both turns succeeded. The source-reading instruction is explicit, so this is an observable demonstration, not a blind grounding evaluation.

The first answer explains the redirect but omits HTTP 400. The follow-up saves a note that includes it. The working branch remains develop; source hashes and the staged index are unchanged. The notes commit matches the disposable local remote. No note was pushed to the real project's GitHub remote.

All scene contact frames were inspected. A recording-start offset was calibrated using both input screenshots; held answer panels use actual captured screenshots directly. Full-file FFmpeg decoding, dimensions, duration and caption count checks passed. See `review-evidence.json` for hashes, source revision and checks.

## Validation and limitations

1. The offline server suite passed 164 tests, and brew wrapper and six pi extension tests passed.
2. The iOS build-for-testing succeeded. All 32 Swift unit tests reported passes. The capture UI test passed on iOS 18.5; the same test skipped when the opt-in configuration was absent, as intended.
3. Apple toolchain teardown hung after test execution and was stopped. Newer simulator capture attempts encountered accessibility-query failures. These outcomes are retained in raw logs; the review does not claim a clean end-to-end exit for every iOS test command or establish the cause as solely an app, runtime or automation defect.
4. Changed Swift files pass SwiftLint and SwiftFormat checks (Swift 6.2 for the existing call-site trailing-comma style). Python tools compile, actual capture/render/verification paths were exercised, and local documentation links and git whitespace checks passed.
5. Generated media and raw sessions stay local. The scripts, captions' source text, capture manifest summary and documentation are committed. Re-running inference may yield different answers; retain the ignored capture directory to regenerate this exact edit.

The product specification, launch-readiness packet and continuous-voice research are in `docs/`. Foreground continuous conversation is acceptable; pocket operation is desirable. No hot-mic feature was implemented and nothing was posted to Hacker News.

Next action: review the two edits for message and pacing, then select narration, TTS or text-only presentation. Public launch still needs a verified first-run provider/setup path and a deliberate submission by the creator.
